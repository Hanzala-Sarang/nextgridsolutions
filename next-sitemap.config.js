/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://nextgrid.solutions', 
  generateRobotsTxt: true,           // Generates robots.txt file also
  changefreq: 'daily',
  priority: 0.7,
};
